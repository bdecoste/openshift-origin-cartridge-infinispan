#!/bin/bash

# Start the application httpd instance

# Exit on any errors
set -e

echo "OPENSHIFT_INFINISPAN_AMQP_HOST=${OPENSHIFT_GEAR_DNS}"
echo "OPENSHIFT_INFINISPAN_AMQP_PORT=${OPENSHIFT_INFINISPAN_AMQP_PROXY_PORT}"
echo "OPENSHIFT_INFINISPAN_OPENWIRE_HOST=${OPENSHIFT_GEAR_DNS}"
echo "OPENSHIFT_INFINISPAN_OPENWIRE_PORT=${OPENSHIFT_INFINISPAN_OPENWIRE_PROXY_PORT}"
